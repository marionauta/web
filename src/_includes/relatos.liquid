<!doctype html>
<html lang=es>

<head>
  <meta charset=utf-8>
  <title>{{ title }}</title>
  <link rel=stylesheet href=/assets/style.css>
  <link rel=stylesheet href=/assets/relatos.css>
</head>

<body>
  <div class=wrapper>
    <a href={{ page.url }} class=date>{{ date | date: "%Y-%m-%d" }}</a>
    <h1>{{ title }}</h1>
    {% if subtitle %}<h2>{{ subtitle }}</h2>{% endif %}
    {{ content }}
  </div>
</body>

</html>
