<!doctype html>
<html>

<head>
  <meta charset=utf-8>
  <title>{{ title }}</title>
  <link rel=stylesheet href=/assets/style.css>
</head>

<body>
</body>
  <div class=wrapper>
    <h1>{{ title }}</h1>
    <ul>
      {% for relato in collections.relatos reversed %}
      <li>
        <a href={{ relato.url }}>{{ relato.data.title }}</a>
      </li>
      {% endfor %}
    </ul>
  </div>
</html>
